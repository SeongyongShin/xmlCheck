package QA.xml;

import java.util.Stack;

public class XmlSolve {
	Stack<String> tag;
	Stack<String> name;
	Stack<String> field;
	public String solve(String xml){
		
		System.out.println(xml.getBytes().toString());
		
		/*
		if(xml.indexOf(0) != '<' && xml.indexOf(1) != '<')
			for(int i=2;;i++) {
				System.out.println(xml.indexOf(i));
				try {
					Thread.sleep(100);
				} catch (InterruptedException e) {
					// TODO Auto-generated catch block
					e.printStackTrace();
				}
				if(xml.indexOf(i) == '<') {
					xml.substring(0, i-1);
					break;
				}
			}
			*/
		return xml;
	}
	
}
